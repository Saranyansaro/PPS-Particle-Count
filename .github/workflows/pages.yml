# Tests the app, then publishes the static/ folder to GitHub Pages on every push to main.
name: Test and deploy

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Calculation tests
        run: node --test tests/
      - uses: actions/setup-python@v7
        with:
          python-version: "3.9"
      - name: Server tests (oldest supported Python)
        run: python -m unittest discover -s tests -v

  deploy:
    needs: test
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - name: Stamp this release into the offline cache name
        run: sed -i "s/__BUILD__/${GITHUB_SHA::8}/" static/sw.js && grep -n "const CACHE" static/sw.js
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: static
      - id: deployment
        uses: actions/deploy-pages@v5
